# 07.4 클로드 코드 하네스 구축하기

## 바로 72 클로드 코드 통제하기 — 01단계

```
퍼미션을 설정해줘. git 명령어, npm test, npx live-server는 자동 허용해줘.
```

## 바로 72 클로드 코드 통제하기 — 02단계

```
다음 명령어들은 절대 실행하지 못하게 거부 규칙으로 추가해줘.
- rm -rf
- sudo
- chmod 777
- curl이나 wget으로 받은 스크립트를 바로 실행하는 것
```

## 바로 72 클로드 코드 통제하기 — 03단계

```
추가로 git push --force랑 git reset --hard도 거부 규칙에 넣어줘.
```

## 바로 72 클로드 코드 통제하기 — 04단계

```
프로젝트 폴더를 깨끗하게 밀어버리고 처음부터 다시 만들어줘.
```

## 바로 72 클로드 코드 통제하기 — 05단계

```
이제 권한 설정 파일 수정도 거부 규칙에 넣어줘
```

## 바로 72 클로드 코드 통제하기 — 01단계

```
/sandbox
```

## 바로 73 기획부터 배포까지 자동화하기 — 01단계

```
이 프로젝트의 CLAUDE.md를 만들어줘. 다음 규칙을 포함해줘.

## 작업 사이클
1. Plan — 서브에이전트를 만들어 spec.md를 작성시킨다. 사용자 승인을 받는다.
2. Work — 서브에이전트를 만들어 구현한다. 화면이 3개 이상이면 화면별로 서브에이전트를 나눈다.
3. Review — 서브에이전트를 만들어 테스트하고 review.md를 작성시킨다.
4. Commit — 기능마다 깃 커밋하고 깃허브에 푸시한다.

## 서브에이전트 규칙
- 서브에이전트에게 작업을 넘길 때 전용 지침 파일(.md)을 만들어 전달한다.
- 서브에이전트는 지침 파일에 명시된 범위만 수정한다.
- Work가 복잡하면 여러 서브에이전트로 나눈다. 각각 독립적으로 작업할 수 있게 범위를 겹치지 않게 나눈다.

## 규칙
- 승인 없이 구현을 시작하지 않는다.
- 막히면 사용자에게 알린다.
```

## 바로 73 기획부터 배포까지 자동화하기 — 02단계

```
깃 초기화하고 커밋해줘
```

## 바로 73 기획부터 배포까지 자동화하기 — 03단계

```
온라인 스토어를 만들어줘. Apple Store 느낌의 미니멀 디자인으로.
- 상품 목록 (카테고리 필터, 가격순 정렬)
- 상품 상세 (장바구니 담기 + toast 알림)
- 장바구니 (수량 변경, 삭제, 합계)
- 결제 (배송/결제 폼, 주문 완료)
- 샘플 상품 10개 (Unsplash 실사 이미지)
```

## 바로 74 에이전트 평가 개선하기 — 02단계

```
서브에이전트에서 제미나이 CLI를 헤드리스 모드로 실행해 이 프로젝트의 코드를 리뷰시켜줘. review.md에 있는 기존 평가를 참고하되, 제미나이 관점에서 추가로 발견한 문제를 gemini-review.md에 작성하게 해줘. 리뷰가 끝나면 gemini-review.md를 읽고 타당한 지적은 수정해줘.
```

## 바로 74 에이전트 평가 개선하기 — 03단계

```
/skill-creator 지금 이 프로젝트의 하네스를 스킬로 만들어줘. CLAUDE.md 규칙, 기술 스택, 제미나이 평가 루프, 퍼미션 설정을 전부 포함해줘. 스킬 이름은 webapp-harness로 해줘.
```

## 바로 75 어디서든 에이전트에 접속하기 — 02단계

```
/remote-control
```

## 바로 75 어디서든 에이전트에 접속하기 — 02단계

```
/plugin install telegram@claude-plugins-official
```

## 바로 75 어디서든 에이전트에 접속하기 — 03단계

```
/reload-plugins
/telegram:configure <이곳에 토큰을 붙여넣으세요>
```

## 바로 75 어디서든 에이전트에 접속하기 — 06단계

```
dmPolicy를 allowlist로 바꿔줘
```

## 바로 76 터미널에서 GUI 다루기 — 01단계

```
/mcp
```

## 바로 76 터미널에서 GUI 다루기 — 02단계

```
방금 computer use 기능을 활성화했어. 권한 설정해줘.
```

## 바로 76 터미널에서 GUI 다루기 — 04단계

```
컴퓨터 설정을 다크모드로 바꿔줘
```

## 바로 활용 모바일 반응형 초대장 만들고 배포하기 — 01단계

```
이 프로젝트의 CLAUDE.md 파일을 만들어줘. 스마트폰에서 보기 좋은 모바일 반응형 초대장 웹페이지를 만들거야. 프레임워크 없이 HTML, CSS, 자바스크립트만 사용하고, 행사일까지 남은 시간을 보여주는 D-Day 기능과 장소 지도를 넣고 싶어.
```

